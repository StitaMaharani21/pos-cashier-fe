# shared/types

Global TypeScript types/utility types not tied to a specific entity or feature (e.g. a generic `Paginated<T>` API envelope). Entity-specific types belong in `entities/<name>/model` instead.
